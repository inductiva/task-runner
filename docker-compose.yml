services:
  task-runner:
      image: inductiva/task-runner:${TAG}
      environment:
        USER_API_KEY: ${INDUCTIVA_API_KEY}
        MACHINE_GROUP_NAME: ${MACHINE_GROUP_NAME}
        HOST_NAME: ${TASK_RUNNER_HOSTNAME:-${HOSTNAME}}
      volumes:
        - ./apptainer:/executer-images
        - workdir:/workdir
      network_mode: host
      privileged: true
      platform: linux/amd64
  file-tracker:
      image: inductiva/file-tracker:${TAG}
      environment:
        USER_API_KEY: ${INDUCTIVA_API_KEY}
      network_mode: host
      volumes:
        - workdir:/workdir

volumes:
  workdir:
